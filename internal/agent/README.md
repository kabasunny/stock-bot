# internal/agent

このディレクトリは、システムの「エージェント」として動作する、主体的・能動的なコンポーネントのロジックを配置します。

エージェントは、アプリケーション全体の「頭脳」であり、取引戦略の実行、外部サービス（Go APIラッパーやPythonシグナル生成サービス）の呼び出し、状態管理などを担当します。

## サブディレクトリ

- `/daytrade`: デイトレード戦略を実行するエージェントの実装を配置します。
- `/swingtrade`: スイングトレード戦略を実行するエージェントの実装を配置します。
